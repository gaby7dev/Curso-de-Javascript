const autenticado = true;

// if(autenticado === true)

// forma correcta
if(autenticado){
    console.log('Si puedes ver netflix');
} else{
    console.log("No estás autenticado, no puedes ver netflix");
}

// Operador ternario
console.log( autenticado ? 'Si está autenticado' : 'No está autenticado' );