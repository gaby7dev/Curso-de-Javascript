// El objeto Math

let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.5); 
resultado = Math.round(2.4); 

// Redondear hacia arriba - ceil (techo)
resultado = Math.ceil(2.5);

// Redondear hacia abajo - floor (suelo)
resultado = Math.floor(2.5);

// Raíz Cuadrada
resultado = Math.sqrt(144); 

// Valor absoluto 
resultado = Math.abs(-500) 

// Potencia
resultado = Math.pow(2,4);

// Valor mínimo
resultado = Math.min(3, 5, 1, 12, -3); 

// Valor máximo
resultado = Math.max(3, 5, 1, 12, -3); 

// Alearorio
resultado = Math.random();

// Aleatorio dentro de nu rango:
resultado = Math.floor( Math.random() * 30 ); // número entre 0 y 30


console.log(resultado);