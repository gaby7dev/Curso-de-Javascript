const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true
}


// Los objetos tiene la sintaxis de punto que nos permite acceder a las propiedades del objeto. EJ:

console.log( producto.precio );

// Otra forma alternativa:
console.log( producto["precio"] );
